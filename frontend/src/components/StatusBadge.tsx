export function StatusBadge({ status }: { status: string }) { const tone=status.toLowerCase().includes('fail')||status==='REJECTED'?'danger':status==='APPROVED'||status==='COMPLETE'||status==='SUCCESS'||status==='ACTIVE'?'success':status==='PROPOSED'||status.includes('RUN')||status.includes('PROCESS')?'info':'neutral'; return <span className={`badge badge-${tone}`}>{status.replaceAll('_',' ')}</span> }
