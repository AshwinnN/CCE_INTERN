import type { ReactNode } from 'react'; import { Button } from './Button'
export function Modal({ title, children, confirmLabel='Confirm', onConfirm, onClose, danger=false, loading=false }: { title:string; children:ReactNode; confirmLabel?:string; onConfirm:()=>void; onClose:()=>void; danger?:boolean; loading?:boolean }) { return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><h3>{title}</h3><button onClick={onClose}>×</button></div><div className="modal-body">{children}</div><div className="modal-actions"><Button variant="secondary" onClick={onClose}>Cancel</Button><Button variant={danger?'danger':'primary'} loading={loading} onClick={onConfirm}>{confirmLabel}</Button></div></div></div> }
