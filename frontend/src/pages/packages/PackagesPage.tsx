import{useEffect,useState}from'react';import{Link}from'react-router-dom';import{listPackages}from'../../api/packages';import type{PackageInfo}from'../../types/api';import{PageHeader}from'../PageHeader';import{Loader}from'../../components/Loader';import{Feedback}from'../../components/Feedback';import{EmptyState}from'../../components/EmptyState'
export function PackagesPage(){const[p,setP]=useState<PackageInfo[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{listPackages().then(r=>setP(r.packages)).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <section className="page"><PageHeader title="Packages" description="View immutable governed context package versions created by the backend lifecycle."/>{error&&<Feedback message={error}/>} {loading?<Loader label="Loading packages…"/>:p.length===0?<EmptyState title="No packages yet" description="Packages are built automatically after terminal proposal decisions."/>:<div className="table-card"><table><thead><tr><th>Name</th><th>Package ID</th><th>Domain ID</th></tr></thead><tbody>{p.map(x=><tr key={x.package_id}><td><Link className="table-link" to={`/packages/${x.package_id}`}>{x.name}</Link></td><td className="mono">{x.package_id}</td><td className="mono">{x.domain_id}</td></tr>)}</tbody></table></div>}</section>}
