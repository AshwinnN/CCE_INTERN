import {useCallback,useEffect,useRef,useState}from'react';import{Link,useParams}from'react-router-dom';import{getIngestionRun}from'../../api/sources';import type{IngestionRun}from'../../types/api';import{PageHeader}from'../PageHeader';import{Button}from'../../components/Button';import{Loader}from'../../components/Loader';import{Feedback}from'../../components/Feedback';import{StatusBadge}from'../../components/StatusBadge'
const terminal=new Set(['COMPLETE','FAILED','CANCELLED','PARTIAL','ERROR']);
export function IngestionRunPage(){const{runId}=useParams();const[run,setRun]=useState<IngestionRun|null>(null),[loading,setLoading]=useState(true),[refreshing,setRefreshing]=useState(false),[error,setError]=useState('');const timer=useRef<number|undefined>();const load=useCallback(async(manual=false)=>{if(!runId)return;if(manual)setRefreshing(true);try{const r=await getIngestionRun(runId);setRun(r);setError('');if(!terminal.has(r.status))timer.current=window.setTimeout(()=>load(false),3000)}catch(e){setError((e as Error).message)}finally{setLoading(false);if(manual)setRefreshing(false)}},[runId]);useEffect(()=>{load();return()=>{if(timer.current)window.clearTimeout(timer.current)}},[load]);if(loading)return <section className="page"><Loader label="Checking ingestion status…"/></section>;return <section className="page"><PageHeader title="Ingestion run" description={runId} action={<Button variant="secondary" loading={refreshing} onClick={()=>load(true)}>Refresh</Button>}/>{error&&<Feedback message={error}/>}<div className="detail-card ingestion-card"><div className="status-line"><span>Status</span><StatusBadge status={run?.status||'UNKNOWN'}/>{run&& !terminal.has(run.status)&&<span className="live-note"><span className="pulse"/> Auto-refreshing</span>}</div>{run&&!terminal.has(run.status)&&<Loader label="Waiting for the next status update…"/>}{run?.error&&<Feedback message={run.error.message}/>}<div className="metrics"><div><span>Objects processed</span><strong>{run?.objects_processed??0}</strong></div><div><span>Objects failed</span><strong>{run?.objects_failed??0}</strong></div></div>{run?.status==='COMPLETE'&&<div className="next-step"><p>Ingestion completed. Knowledge proposals can now be reviewed when available.</p><Link to="/proposals"><Button>Open Knowledge</Button></Link></div>}</div></section>}
