# CCE Skills

Skills are versioned agent assets. Each skill owns a bounded reasoning
procedure in `SKILL.md`, optional deterministic helpers in `scripts/`, and
references that are loaded only when the skill requires them.
